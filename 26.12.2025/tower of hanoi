class Solution {
    public void toh(int n, char src, char aux, char dest) {
        if (n == 0) return;
        toh(n - 1, src, dest, aux);
        System.out.println(src + " -> " + dest);
        toh(n - 1, aux, src, dest);
    }
}
